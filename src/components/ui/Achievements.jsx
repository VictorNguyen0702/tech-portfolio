import {Card} from "@/components/ui/card";
import {Link} from "lucide-react";

const Achievements = () => {
  
  return (
    <div className="p-6 md:p-8 bg-[var(--light-color)] rounded-xl border border-neutral-800 w-full max-w-full mx-auto">
      {/* Header */}
      <h2 className="text-xl font-bold flex items-center gap-2">
        <p className= "text-[var(--dark-color)]">My Achievements</p>
      </h2>
      <p className="text-sm text-[var(--dark-color)]">My most notable achievements:</p>
      
      {/* Grid for Tech Stack */}
    </div>
  )
}

export default Achievements